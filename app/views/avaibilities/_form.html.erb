<%= form_for([@room,@avaibility], :html => {:name=>"f", :onsubmit => "return validate1()"}) do |f| %>
  <% if avaibility.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(avaibility.errors.count, "error") %> prohibited this avaibility from being saved:</h2>

      <ul>
      <% avaibility.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <md-subheader class="md-primary">From </md-subheader><input type="text"  class="datepicker" name="avaibility[datef]" id="avaibility_datef" required></p> 
              
  <md-subheader class="md-primary">To </md-subheader> <input type="text" name="avaibility[datet]"  class="datepicker" id="avaibility_datet" required></p> 


  <div class="field">
    <%= f.label :pricepn %>
    <%= f.text_field :pricepn %>
  </div>


  <div class="field">
    <%= f.label :available %>
    <%= f.check_box :available %>
  </div>

 <div class="errorHand"></div>
  <div class="actions">
    <%= f.submit "Set", class: "btn btn-default", id: 'btn-sign-up'  %>

        <md-button class="md-accent md-raised md-hue-3" href="/hotelprofile">Back</md-button>

  </div>

 
<% end %>
<script>
$(".errorHand").hide();
</script>