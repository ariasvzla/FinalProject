<p id="notice"><%= notice %></p>
<h1>Bookings</h1>

<table>
  <thead>
    <tr>
      <th>Start</th>
      <th>Return</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  <%# transverse the booking table to display data stored in it%>
 
      <tr>
     
         <%@var=DateTime.parse(@booking.stdate)%>
         <%@var2=DateTime.parse(@booking.endate)%>
      
        
    
        <td><%= link_to 'Edit', edit_room_booking_path(@booking, @room) %></td>
        <td><%= link_to 'Destroy', @booking, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>


     <%@c=@var2-@var%>
     <%=@c.to_i%>
  </tbody>
</table>
<div id="total"></div>

<%= link_to 'Edit', edit_booking_path(@booking) %> |
<%= link_to 'Back', bookings_path %>
